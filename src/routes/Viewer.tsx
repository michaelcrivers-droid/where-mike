/**
 * The public route. Everything the visitor sees lives in the viewer component;
 * this file exists so the router has a stable place to point at.
 */
export { default } from '@/components/viewer/Viewer'
